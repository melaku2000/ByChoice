@model IEnumerable<ByChoice.Models.AgentCatagory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AgentCatagoryType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AgentCatagoryType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgentCatagoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgentCatagoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgentCatagoryId })
        </td>
    </tr>
}

</table>
