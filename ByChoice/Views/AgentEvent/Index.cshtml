@model IEnumerable<ByChoice.Models.AgentEvent>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.TaxNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Event.Location)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationUser.TaxNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Event.Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgentEventId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgentEventId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgentEventId })
        </td>
    </tr>
}

</table>
