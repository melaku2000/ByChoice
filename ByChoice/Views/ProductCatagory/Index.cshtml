@model IEnumerable<ByChoice.Models.ProductCatagory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductCatagoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCatagoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCatagoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCatagoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCatagoryId })
        </td>
    </tr>
}

</table>
